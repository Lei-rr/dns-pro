/**
 * Compatibility barrel — re-exports kernel contracts.
 * Prefer importing from `src/kernel/index.js` for new code.
 * This file remains so existing `../contracts/index.js` imports keep working.
 */
export * from '../kernel/contracts.js'

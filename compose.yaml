services:
  dns-pro:
    build:
      context: .
      dockerfile: Dockerfile
    image: dns-pro:local
    container_name: dns-pro
    restart: unless-stopped
    ports:
      - "2022:2022"
    volumes:
      - ./data:/app/data
